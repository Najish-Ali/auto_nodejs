# Node.js App Settings
PORT=3000
NODE_ENV=production

# Database (if used)
DB_HOST=your-db-host
DB_USER=your-db-user
DB_PASS=your-db-password

# Security & API Keys (Replace with real keys)
JWT_SECRET=your-jwt-secret
API_KEY=your-api-key

